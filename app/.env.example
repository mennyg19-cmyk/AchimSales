# Preview / local only. Never set APP_ENV=production on this rebuild until Menny signs off.
APP_ENV=preview
# Required when APP_ENV=production. Preview may use a local-only default.
SESSION_SECRET=
# Reporting doorway — do not point this at reports.achimonline.com. Slice 1 does not call it.
# REPORTING_API_BASE_URL=https://achim-reporting-api-test-hpadbffpcwe0dnga.westus3-01.azurewebsites.net
# REPORTING_API_KEY=
