# v3 Sales Reports - runtime dependencies
# Pinned ranges match the repo-root production requirements.txt.
Flask>=3.1,<4
Jinja2>=3.1
msal>=1.31,<2
python-dotenv>=1.0
requests>=2.31
gunicorn>=22.0

# Data / reporting
pandas>=2.0
openpyxl>=3.1

# Background scheduling (in-process, single B1 instance)
APScheduler>=3.10,<4

# Microsoft Graph (SharePoint + email) reuse live app registration
azure-identity>=1.15,<2

# tzdata: cross-platform IANA zoneinfo fallback for slim Docker images
tzdata>=2024.1

# --- dev / test only ---
pytest>=8.0
