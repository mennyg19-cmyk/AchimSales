# Python
__pycache__/
*.pyc
.pytest_cache/

# Local data (SQLite + WAL/SHM). precious.db is replicated by Litestream in prod.
.data/
*.db
*.db-wal
*.db-shm

# Front-end build
node_modules/
# NOTE: web/static_dist/ IS committed. Azure (Oryx) only runs pip, not esbuild,
# so the built bundle must ship in git. Rebuild with `npm run build` before commit.

# Env
.env
