# D365 Finance & Operations
D365_ENV_URL=https://your-org.operations.dynamics.com
D365_COMPANY_ID=

# Azure AD / MSAL (client credentials)
GRAPH_TENANT_ID=your-tenant-id
GRAPH_CLIENT_ID=your-client-id
GRAPH_CLIENT_SECRET=your-client-secret

# SharePoint (for runbook upload)
SP_SITE_URL=https://your-org.sharepoint.com/sites/your-site
DriveRootPath=D365 F&O

# Email for customer-filtered Ordered --email runs (optional)
# AMAZON_EMAIL_RECIPIENTS=user1@company.com,user2@company.com

# Option A: Microsoft Graph (recommended for business; no app password)
# Uses same GRAPH_TENANT_ID, GRAPH_CLIENT_ID, GRAPH_CLIENT_SECRET. App needs Mail.Send + admin consent.
# AMAZON_EMAIL_FROM=reports@company.com

# Option B: SMTP (e.g. if Graph not set up)
# SMTP_USER=reports@company.com
# SMTP_PASSWORD=your-app-password
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587

# Alerting (runbook failure / warning notifications)
# Comma-separated list of admin email addresses. If empty, alerts are logged only.
# ALERT_RECIPIENTS=admin@company.com,ops@company.com

# Live vs /test parity runner (python -m tools.parity)
# PARITY_BASE_URL=https://reports.achimonline.com
# PARITY_AUTH=cookie
# PARITY_LIVE_COOKIE=...          # browser "session" cookie value after live login
# PARITY_TEST_COOKIE=...          # browser "v3_session" cookie value after /test login
# Local only: PARITY_AUTH=dev (needs DEV_BYPASS_AUTH on live + auth_mode=dev on /test)

# Mounts (Azure App Service / gunicorn wsgi:application)
# V3_MOUNT_ENABLED=1
# V3_URL_PREFIX=/test
# BETA_MOUNT_ENABLED=1          # / is Beta; live OData moves to /legacy
# LEGACY_URL_PREFIX=/legacy
# BETA_URL_PREFIX=/beta         # old bookmarks 302 to the same path without /beta
# BETA_PRECIOUS_DB_PATH=/tmp/betadata/precious.db
# BETA_CACHE_DB_PATH=/tmp/betadata/cache.db
# LITESTREAM_AZURE_BETA_PATH=beta-precious.db
# REBUILD_MOUNT_ENABLED=0
# REBUILD_MOUNT_PATH=/test-next
