[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    freeze_date(date_str): freeze get_today_eastern() to the given YYYY-MM-DD date
    edge_case: marks a test as an edge case scenario
addopts = -v --tb=short
